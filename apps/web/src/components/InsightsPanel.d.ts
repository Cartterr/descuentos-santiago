import type { DashboardMetrics } from '../types';
type InsightsPanelProps = {
    metrics: DashboardMetrics | null;
};
export declare function InsightsPanel({ metrics }: InsightsPanelProps): import("react").JSX.Element;
export {};
