import type { DashboardMetrics } from '../types';
type MetricsStripProps = {
    metrics: DashboardMetrics | null;
};
export declare function MetricsStrip({ metrics }: MetricsStripProps): import("react").JSX.Element;
export {};
